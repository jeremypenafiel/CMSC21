#include <stdio.h>
int main (void){
    int firstNum, secondNum, difference;

    firstNum = 87;
    secondNum = 15;
    difference = firstNum - secondNum;

    printf("The difference of the two numbers is %d", difference);

    return 0;
}
